package com.example.debts.repositories

import com.example.debts.cm.models.CheckModel
import com.example.debts.database.DebtDao
import javax.inject.Inject

//class CheckRepository @Inject constructor(private val apiService: ApiService, private val dao: DebtDao)
//{
////    suspend fun getCheckingList() = dao.getCheckingList()
////    suspend fun sendCheckingListApi(body: List<CheckModel>) = apiService.sendCheckingUpdates(body)
//}